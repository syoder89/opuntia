Index: build_dir/feeds/packages/net/frr/files/daemons
===================================================================
--- build_dir.orig/feeds/packages/net/frr/files/daemons
+++ build_dir/feeds/packages/net/frr/files/daemons
@@ -1,9 +1,9 @@
 # The watchfrr and zebra daemons are always started.
 #
-bgpd=no
-ospfd=no
+bgpd=yes
+ospfd=yes
 #ospfd_instances=1,20
-ospf6d=no
+ospf6d=yes
 ripd=no
 ripngd=no
 isisd=no
@@ -14,9 +14,9 @@ eigrpd=no
 babeld=no
 sharpd=no
 pbrd=no
-bfdd=no
+bfdd=yes
 fabricd=no
-vrrpd=no
+vrrpd=yes
 
 #
 # If this option is set the /etc/init.d/frr script automatically loads
Index: build_dir/feeds/packages/net/frr/files/frr
===================================================================
--- build_dir.orig/feeds/packages/net/frr/files/frr
+++ build_dir/feeds/packages/net/frr/files/frr
@@ -19,8 +19,20 @@ else
 	. "/usr/sbin/frrcommon.sh"
 fi
 
+migrate_quagga_configs() {
+	if [ -d /etc/quagga ]; then
+		logger "Migrating Quagga configuration files to FRR..."
+		for file in /etc/quagga/*.conf ; do
+			logger "Moving file $file"
+			mv "$file" /etc/frr/
+		done
+		logger "Remaining Quagga files can be found in /etc/quagga.bak"
+		mv /etc/quagga /etc/quagga.bak
+	fi
+}
 
 start() {
+        migrate_quagga_configs
 	daemon_list daemons
 	watchfrr_options="$watchfrr_options $daemons"
 	daemon_start watchfrr
Index: build_dir/feeds/packages/net/frr/patches/0001-convert-wtf-message.patch
===================================================================
--- /dev/null
+++ build_dir/feeds/packages/net/frr/patches/0001-convert-wtf-message.patch
@@ -0,0 +1,15 @@
+Index: frr-7.2.1/watchfrr/watchfrr.c
+===================================================================
+--- frr-7.2.1.orig/watchfrr/watchfrr.c
++++ frr-7.2.1/watchfrr/watchfrr.c
+@@ -533,7 +533,9 @@ static int wakeup_init(struct thread *t_
+ static void restart_done(struct daemon *dmn)
+ {
+ 	if (dmn->state != DAEMON_DOWN) {
+-		zlog_warn("wtf?");
++		zlog_warn(
++			"Daemon: %s: is in %s state but expected it to be in DAEMON_DOWN state",
++			dmn->name, state_str[dmn->state]);
+ 		return;
+ 	}
+ 	if (dmn->t_wakeup)
